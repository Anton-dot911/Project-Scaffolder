# CLAUDE.md — {{project_name}}

## What this project is

TODO: one paragraph on what {{project_name}} does and for whom.

## Stack

- Node 22, TypeScript strict, commander (CLI), tsup (build)
- Tests: vitest
- Lint/format: ESLint + Prettier

## Commands

- Dev run: `pnpm dev -- <args>`
- Build: `pnpm build` | Tests: `pnpm test`
- Lint: `pnpm lint` | Typecheck: `pnpm typecheck`

## Structure

```
src/
  cli.ts      # commander entry point
  greet.ts    # example module; replace with real functionality
tests/        # vitest tests
```

## Hard rules

TODO: fill in per project.
